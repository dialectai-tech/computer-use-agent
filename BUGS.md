# Known Bugs and Issues

## Critical Bugs

### ✅ BUG-001: Automatic context reset not triggering at threshold
**Status**: ✅ Fixed (commit ac64c77)
**Priority**: Critical
**Found in**: Test run 2026-02-08 11:58:46

**Description**:
Automatic context reset set to trigger at 200,000 tokens but never triggered even when input tokens reached 210,390 at iteration 21.

**Evidence**:
```
Iteration 21:
Input Tokens: 210,390
Threshold: 200,000
Expected: Auto reset at iteration ~17-18
Actual: No reset triggered
```

**Root Cause**: TBD (needs investigation)

**Suspected Issue**:
- Auto-reset check may not be called at the right time
- Threshold comparison logic may be broken
- Check happens before pruning but threshold check uses post-pruning stats

**Solution**: ✅ FIXED
- Moved auto-reset check to AFTER breakdown calculation
- Pass current iteration's input tokens instead of cumulative total
- Added debug logging to track threshold checks

---

### ✅ BUG-007: Empty messages after context reset
**Status**: ✅ Fixed (commits 31115d8, fd50030, 5bc50d7, 11ba1e3)
**Priority**: Critical
**Found in**: After implementing auto-reset

**Description**:
After context reset, AWS Bedrock rejected messages with error "messages.2 is empty".

**Root Cause**:
Multiple issues:
1. Screenshot stripping created empty content arrays
2. first_user_message was stored as reference and got mutated during stripping
3. Reset reused the mutated reference

**Solution**: ✅ FIXED
- Only strip screenshots from messages with other content
- Keep screenshots if message would become empty
- Deep copy first_user_message when storing initially
- Deep copy again when using in reset
- Keep last 2 messages intact during stripping

---

### 🔴 BUG-002: Invalid CSS selectors generated by AI
**Status**: 🔴 Open
**Priority**: High
**Found in**: Iteration 2

**Description**:
AI generates jQuery-style selectors that are invalid in standard CSS/DOM APIs.

**Evidence**:
```
DOM Click: button:contains('START')
Error: 'button:contains("START")' is not a valid selector
```

**Root Cause**:
- `:contains()` is jQuery syntax, not valid CSS
- DOM manipulation tool doesn't validate selectors before execution
- AI model may be trained on jQuery examples

**Impact**:
- Wastes iterations on failed clicks
- AI gets confused by selector errors

**Solution Options**:
1. Add selector validation with helpful error messages
2. Update system prompt to explicitly forbid jQuery-style selectors
3. Add selector correction/translation layer (`:contains()` → `[text*="..."]`)
4. Better examples in tool documentation

---

### 🟡 BUG-003: Generic selectors cause timeouts
**Status**: 🟡 Open
**Priority**: Medium
**Found in**: Iteration 19

**Description**:
AI uses overly generic selectors like `input` that match multiple elements or hidden elements, causing timeouts.

**Evidence**:
```
DOM Click: input
Error: Page.click: Timeout 5000ms exceeded
```

**Root Cause**:
- AI doesn't provide specific enough selectors
- No guidance to use more specific selectors (id, classes, attributes)
- DOM Find doesn't return which selector to use, just that element exists

**Impact**:
- Wasted iterations
- Stuck loops

**Solution Options**:
1. DOM Find should return the specific selector to use
2. Add selector specificity validation (reject single-tag selectors)
3. Better AI guidance on selector specificity
4. Return top 3 matching elements with their selectors

---

## Misleading/Confusing Output

### 🟢 BUG-004: Misleading "Context: N screenshots" message
**Status**: 🟢 Open (Low Priority)
**Priority**: Low
**Found in**: All iterations

**Description**:
Console shows `Context: 5 screenshots (5 important)` but only 1 screenshot (1,406 tokens) is actually sent to API.

**Evidence**:
```
Context: 5 screenshots (5 important)
Screenshots: 1,406 tokens (constant)
```

**Root Cause**:
This is `screenshot_history` (context window tracking) being displayed, not actual screenshots sent to API. Two different concepts using same terminology.

**Impact**:
- Confusing to users monitoring token usage
- Makes it seem like optimization isn't working

**Solution**:
- Rename display to "Screenshot History: 5 (for context tracking)"
- Or: "Screenshot Memory: 5 | API Sends: 1"
- Or: Remove this display entirely since it's internal tracking

---

## Performance Issues

### 🟡 BUG-005: Token growth still excessive despite optimizations
**Status**: 🟡 Open
**Priority**: Medium
**Analysis Needed**

**Description**:
Despite screenshot stripping and message pruning, tokens still grew from 5K to 210K over 20 iterations.

**Breakdown**:
- Iteration 1: 5,469 tokens
- Iteration 11: 82,899 tokens
- Iteration 21: 210,390 tokens
- Average growth: ~10,000 tokens/iteration after iteration 10

**Evidence of Working Optimizations**:
- ✅ Screenshots: Constant at 1,406 tokens
- ✅ System prompt: Cached (0 tokens after iteration 1)
- ✅ Message pruning: Active (kept 22 messages with 10 turns)

**Where are tokens coming from?**:
- AI Responses: Growing (208,984 tokens at iteration 21)
- Page text: 13,392 tokens at iteration 6 (sent once on navigation)
- Message pruning keeps 10 turns = 20 messages of conversation history

**Analysis**:
With 10 message turns and verbose AI responses (~2,000 tokens each at end):
- 10 turns × 2 messages/turn = 20 messages
- 20 messages × ~2,000 tokens = 40,000 tokens from responses
- But we're seeing 208,984 tokens from AI responses (5x more!)

**Root Cause Hypothesis**:
AI responses include tool_use blocks with full JSON structures that accumulate. The "AI Responses" metric may be overcounting or message pruning isn't aggressive enough.

**Solution Options**:
1. Auto-reset should have prevented this (see BUG-001)
2. Reduce max_message_turns to 3-5 (was 10 in test)
3. Investigate why AI response tokens are so high
4. Strip tool_use blocks from old messages (keep only text)

---

## Test Configuration Issues

### 🔵 BUG-006: Test used max_message_turns=10 (too high)
**Status**: 🔵 Info
**Priority**: N/A (user error)

**Description**:
Test command used `--max-message-turns 10` which keeps more history than recommended.

**Impact**:
- More token accumulation than necessary
- Masks effectiveness of optimizations

**Recommendation**:
Use `--max-message-turns 3` (default) for normal operations.

---

## Investigation Needed

### 🟣 INVESTIGATE-001: Why didn't auto-reset trigger?
**Priority**: Critical
**Next Steps**:
1. Check if `_auto_reset_context_if_needed()` is being called
2. Add debug logging to show threshold checks
3. Verify `input_tokens` value being compared
4. Check if comparison happens before/after pruning

### 🟣 INVESTIGATE-002: Why are AI response tokens so high?
**Priority**: High
**Next Steps**:
1. Examine conversation dump JSON files
2. Count actual tokens in tool_use blocks
3. Verify token estimation vs actual tokens
4. Check if tool results are being counted as "AI responses"

---

## Summary

**Fixed**: 3 (BUG-001 ✅, BUG-007 ✅, BUG-008 ✅)
**Critical Issues**: 1 (BUG-002)
**High Priority**: 1 (BUG-005)
**Medium Priority**: 1 (BUG-003)
**Low Priority**: 1 (BUG-004)
**Info**: 1 (BUG-006)

**Next Actions**:
1. ✅ Fixed BUG-001: Auto-reset now uses current iteration tokens
2. ✅ Fixed BUG-007: Deep copy prevents message mutations
3. 🔴 Fix BUG-002: Add selector validation or better AI guidance
4. 🟡 Fix BUG-003: Improve DOM Find to return specific selectors
5. 🟢 Fix BUG-004: Update display text to be less confusing
6. 🧪 Test complete solution with final command

---

### ✅ BUG-008: Empty continuation after auto-reset  
**Status**: ✅ Fixed (commit 14c31b0)  
**Priority**: Critical  
**Found in**: After fixing BUG-007

**Description**:
Even with BUG-007 fixed, empty message error still occurred immediately after auto-reset triggered.

**Root Cause**:
After context reset:
1. `last_tool_uses` is cleared in reset_context()
2. Next iteration calls `create_continuation_request`
3. Builds `tool_result_content` by iterating empty `last_tool_uses` list
4. Appends user message with empty content array
5. AWS Bedrock rejects empty content arrays

**Solution**: ✅ FIXED
1. Added `just_reset` flag to track when reset just happened
2. After reset, start fresh iteration (take screenshot, continue normally)
3. Safety check in `create_continuation_request`: if `tool_result_content` empty, add placeholder text

**Verification**: ✅ Tested at 2026-02-08 12:32
- Auto-reset triggered at iteration 6 (20,454 tokens)
- Printed "Starting fresh iteration after context reset..."
- No empty message errors
- Continued normally for 4 more iterations
